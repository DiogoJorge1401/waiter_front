import styled from 'styled-components';

export const Container = styled.main`
  width: 100%;
  max-width: 76rem;

  margin-top: 2.5rem;
  margin-inline: auto;
  padding: 0.5rem;

  display: flex;
  justify-content: center;
  gap: 2rem;
`;
